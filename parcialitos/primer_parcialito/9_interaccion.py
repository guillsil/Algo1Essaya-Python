"""Se desea programar un juego en el cual se lanza repetidas veces un dado y que se "gana" cumpliendo algunas
de las siguientes situaciones:
Si el número de veces que se tiró el dado hasta el momento módulo 6 y el último valor lanzado en el dado son iguales.
Si se lanza el mismo valor dos veces seguidas.
Se debe imprimir por pantalla al finalizar, un mensaje indicando la cantidad de tiradas que costó ganarlo.
Nota: Existe la función randint(min, max) del módulo random que devuelve un número entero entre min y max cada
vez que se utiliza. Por ej: random.randint(1,6) -> 4, random.randint(1,6) -> 2, random.randint(1,6) -> 3"""
